# 叠影拼图 — 产品需求文档

## 1. 项目概述

| 项 | 说明 |
|---|---|
| 产品名称 | 叠影拼图 |
| 平台 | 微信小游戏 |
| AppID | `wx3adead8d634a2485` |
| 方向 | 横屏 |
| 类型 | 闯关类空间推理拼图 |
| 核心玩法 | 对照谜底，用材料栏碎片在操作区叠加拼出相同图案 |

---

## 2. 玩法规则

### 2.1 基本机制

- 每关有一个 **谜底图案**（完整目标图），始终可见。
- 系统提供若干 **彩色碎片**，玩家拖入 **操作区**（10×10 网格）进行叠加。
- 碎片为 **不透明叠加**：上层完全遮盖下层颜色。
- 碎片 **不是** 从谜底直接切分而来，包含大量 **干扰块**，需判断位置与层级。
- 玩家需结合空间想象，选择正确碎片并调整叠放顺序，使操作区最终与谜底一致。

### 2.2 胜负判定

- 点击 **提交** 后，系统将操作区当前叠加结果与谜底逐格比对。
- 完全一致则过关，弹窗提示并进入下一关。
- 不一致则提示「图案不一致，请继续调整」。
- 共 **20 关**，全部通关后提示「全部通关」。

### 2.3 关卡难度

- 关卡 1→20，碎片数量从 **20 块递增至 30 块**。
- 约 55% 为构成谜底的正确块，其余为干扰块。
- 关卡数据由种子随机生成，同关卡可复现。

---

## 3. 界面流程

### 3.1 主界面

进入游戏后首先显示主界面：

| 按钮 | 说明 |
|---|---|
| 继续游戏 | 有存档时显示，文案含当前关卡号；恢复上次保存的关卡、碎片布局与材料栏滚动位置 |
| 新游戏 | 清除存档，从第 1 关重新开始 |

- 无存档时仅显示「新游戏」。
- 主界面全屏 Canvas 绘制，深色背景 + 标题 + 按钮。

### 3.2 游戏界面（横屏三栏）

```
┌──────────┬────────────────────┬──────────┐
│  左栏    │      中栏          │  右栏    │
│  关卡信息│    操作区          │  材料栏  │
│  谜底    │   (主面板)         │  2~3列   │
│  操作按钮│                    │  可滚动  │
└──────────┴────────────────────┴──────────┘
```

#### 左栏（约 24% 屏宽 + 安全区）

| 区域 | 内容 |
|---|---|
| 顶部 | 当前关卡（第 X/20 关）、已放置数量；右上角 **主界面** 按钮 |
| 中部 | 谜底预览（最大约 170px，居中卡片） |
| 底部 | 选中工具栏（上移 ↑ / 下移 ↓ / 收回 / 取消）、重置 / 提交 |

- 谜底 **固定显示**，不可收起。
- 左侧预留 **安全区**（灵动岛/刘海）。
- 点击 **主界面** → 二次确认 → 保存进度并返回主界面。

#### 中栏（操作区）

- 10×10 正方形网格面板，尽量占满中间可用空间。
- 棋盘格底色 + 已放置碎片 + **网格线置顶**（盖在碎片之上）。
- 格子与碎片 **1:1 比例对齐**。
- 放置/拖拽时显示半透明预览；进入操作区时面板 **金色边框高亮**。
- 选中碎片时操作区 **局部蒙层**，选中块高亮，其余块半透明。

#### 右栏（材料栏，约 28% 屏宽）

- **2~3 列** 大格背包（按屏宽自适应，单格 ≥ 56px 时优先 3 列）。
- 仅显示 **未放置** 的碎片，**无编号**。
- 碎片在格内居中，带圆角槽位框。
- 支持 **上下滑动** 浏览；滑动与长按/拖拽分离（滑动阈值 8px）。
- **不可收起**。
- 顶部显示「材料栏 (N)」及滚动条指示。

---

## 4. 交互需求

### 4.1 材料栏 → 操作区

| 行为 | 说明 |
|---|---|
| 长按 | 约 380ms 后整格 **悬浮抬起**（含槽位框），轻震动 |
| 拖动 | 悬浮后移动超过 2px 进入拖拽；手指跟随，带缩放与阴影 |
| 过渡 | 进入操作区时格子尺寸与透明度 **平滑过渡**（boardBlend 动画） |
| 放置 | 在操作区内松手 → 按网格吸附放置 |
| 取消 | 在操作区外松手 → 碎片 **不放置**，归位材料栏 |
| 选中 | 放置完成后 **不保持选中** |
| 滑动 | 短按后在材料栏内滑动 → 滚动列表（不与长按冲突） |

### 4.2 操作区内移动

| 行为 | 说明 |
|---|---|
| 点击 | 点击已放置碎片 → **选中** |
| 拖动 | 移动超过阈值（8px）→ 进入拖拽，碎片跟随并 **网格吸附** |
| 放置 | 在操作区内松手 → 吸附到新网格位置 |
| 取消 | 拖到操作区外松手 → **归位** 到原位置 |
| 选中 | 拖动结束后 **取消选中** |

### 4.3 层级调整

- 选中操作区碎片后，左栏显示工具栏：
  - **上移 ↑** 与上一层交换 z 值
  - **下移 ↓** 与下一层交换 z 值
  - **收回** 移回材料栏
  - **取消** 清除选中
- 未选中时显示「取消选中」宽按钮。
- 点击操作区空白格清除选中。

### 4.4 拖拽反馈

- 长按/拖动拿起时 **轻震动**（`wx.vibrateShort`）。
- 材料栏拖拽显示 **整格浮动槽位** + 金色边框。
- 进入操作区可放置时，面板边框 **高亮**（金色）。
- 选中/预览时，操作区 **局部蒙层**。

### 4.5 其他操作

| 操作 | 说明 |
|---|---|
| 重置 | 二次确认后清空本关已放置碎片，并保存 |
| 提交 | 比对谜底，过关弹窗 + 下一关 |
| 主界面 | 二次确认后保存完整进度并返回主界面 |
| 新游戏 | 清除存档，从第 1 关开始 |
| 继续游戏 | 恢复存档中的关卡、碎片位置/层级、材料栏滚动 |

---

## 5. 存档

### 5.1 存储键

`wx.setStorageSync('stackpuzzle_save')`

### 5.2 存档内容

```json
{
  "level": 3,
  "materialScroll": 120,
  "pieces": [
    { "id": 0, "placed": true, "gridX": 2, "gridY": 3, "z": 1 }
  ]
}
```

### 5.3 存档时机

- 进入新关卡 / 过关进入下一关
- 重置本关后
- 返回主界面确认时
- 新游戏时 **清除** 存档

### 5.4 恢复规则

- **继续游戏**：按存档还原关卡与所有碎片状态。
- **新游戏**：清除存档，生成第 1 关。

---

## 6. 数据与算法

### 6.1 网格规格

- 操作区 / 谜底：10×10
- 材料栏展示：自适应大格，碎片按形状 bounds 居中
- 碎片形状：多种多格形状（1~5 格），随机颜色

### 6.2 关卡生成流程

1. 按关卡种子随机生成正确碎片并叠加到空网格，形成谜底。
2. 生成干扰碎片。
3. 全部碎片按 id 编号（0, 1, 2…）。
4. 存储：谜底、碎片列表（含 cells、color、isCorrect 等）。

### 6.3 叠加规则

- 按 z 值从低到高绘制。
- 上层颜色完全覆盖下层。
- 判定以最终网格颜色与谜底逐格比较为准。

---

## 7. 技术实现

### 7.1 技术约束

| 项 | 说明 |
|---|---|
| 框架 | 微信小游戏原生（`compileType: game`） |
| 入口 | `game.js` → `js/main.js` |
| 页面方向 | `game.json` → `deviceOrientation: landscape` |
| 渲染 | 全屏 **Canvas 2D** 单画布绘制（主界面 + 游戏界面） |
| 高清 | 按 `pixelRatio` 缩放 Canvas，每帧 `setTransform` 重置 |
| 输入 | `wx.onTouchStart/Move/End` 统一处理 |
| 坐标 | 逻辑像素；抓取点偏移含格子内相对位置，保证跟手 |
| 存档 | `stackpuzzle_save`（见 §5） |
| 最低版本 | 横屏需微信 8.0.31+ |

### 7.2 目录结构

```
game.js                 # 入口
game.json               # 横屏配置
js/
  main.js               # 主循环、屏幕切换
  render.js             # Canvas 初始化、pixelRatio
  puzzle/
    engine.js           # 游戏状态、放置、层级、提交、存档
    levelGen.js         # 20 关种子生成
    shapes.js           # 碎片形状与颜色
    rng.js              # 确定性随机
    layout.js           # 三栏布局与安全区
    render.js           # 主界面 + 游戏界面绘制
    input.js            # 触摸、长按、拖拽、滚动
```

### 7.3 渲染策略

- 按需渲染：`needsRender` 标志 + 拖拽/悬浮/动画时持续刷新。
- 操作区：棋盘格底 → 已放置碎片（按 z 排序）→ 选中蒙层 → 网格线置顶。
- 材料栏：裁剪区域 + 滚动偏移 + 滚动条与渐隐指示。

---

## 8. 非功能需求

- 操作区与谜底格子 **比例一致**，拖拽定位准确。
- 材料栏滚动与长按/拖拽 **互不干扰**。
- 适配刘海、灵动岛、胶囊按钮安全区（`wx.getWindowInfo().safeArea`）。
- 横屏三栏布局自适应不同屏幕宽度。
- 手机端 Canvas **清晰不模糊**（pixelRatio 适配）。

---

## 9. 功能清单（当前版本）

- [x] 主界面（继续游戏 / 新游戏）
- [x] 返回主界面（二次确认 + 保存）
- [x] 完整存档（关卡 + 碎片布局 + 材料栏滚动）
- [x] 20 关闯关
- [x] 横屏三栏布局
- [x] 谜底固定预览
- [x] 材料栏 2~3 列大格 + 滚动
- [x] 材料栏长按悬浮整格 → 拖拽放置
- [x] 材料 → 操作区尺寸过渡动画
- [x] 操作区网格吸附与跟手偏移
- [x] 操作区网格线置顶
- [x] 点击选中 + 相邻层交换（上移/下移）
- [x] 选中蒙层与高亮
- [x] 提交判定与过关/通关弹窗
- [x] 重置本关（二次确认）
- [x] 安全区适配
- [x] pixelRatio 高清渲染

---

## 10. 后续可扩展（未实现）

- 关卡星级 / 计时 / 提示系统
- 音效与动画 polish
- 关卡编辑器
- 分享与排行榜
- 云存档

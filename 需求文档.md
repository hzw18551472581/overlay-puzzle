# 叠影拼图 — 产品需求文档

## 1. 项目概述

| 项 | 说明 |
|---|---|
| 产品名称 | 叠影拼图 |
| 平台 | 微信小程序 |
| 方向 | 横屏 |
| 类型 | 闯关类空间推理拼图 |
| 核心玩法 | 对照谜底，用材料栏碎片在操作区叠加拼出相同图案 |

---

## 2. 玩法规则

### 2.1 基本机制

- 每关有一个 **谜底图案**（完整目标图），始终可见，不可收起。
- 系统提供若干 **彩色碎片**，玩家拖入 **操作区**（10×10 网格）进行叠加。
- 碎片为 **不透明叠加**：上层完全遮盖下层颜色。
- 碎片 **不是** 从谜底直接切分而来，包含大量 **干扰块**，需判断位置与层级。
- 玩家需结合空间想象，选择正确碎片并调整叠放顺序，使操作区最终与谜底一致。

### 2.2 胜负判定

- 点击 **提交** 后，系统将操作区当前叠加结果与谜底逐格比对。
- 完全一致则过关，自动存档并进入下一关。
- 共 **20 关**，通关后提示「全部通关」。

### 2.3 关卡难度

- 关卡 1→20，碎片数量从 **20 块递增至 30 块**。
- 约 55% 为构成谜底的正确块，其余为干扰块。
- 关卡数据由种子随机生成，同关卡可复现。

---

## 3. 界面布局（横屏三栏）

```
┌──────────┬────────────────────┬──────────┐
│  左栏    │      中栏          │  右栏    │
│  关卡信息│    操作区          │  材料栏  │
│  谜底    │   (主面板)         │  4列背包 │
│  操作按钮│                    │  可收起  │
└──────────┴────────────────────┴──────────┘
```

### 3.1 左栏

| 区域 | 内容 |
|---|---|
| 顶部 | 当前关卡（第 X/20 关）、已放置数量 |
| 中部 | 谜底预览（较小，约为操作区 40% 高度，最大约 104px） |
| 底部 | 选中工具栏（↑↓ 调层、收回）、取消 / 重置 / 提交 |

- 谜底 **固定显示**，不提供收起功能。
- 左侧预留 **安全区**（灵动岛/刘海）。

### 3.2 中栏（操作区）

- 10×10 正方形网格面板，尽量占满中间可用空间。
- 格子与碎片 **1:1 比例对齐**。
- 放置时显示网格线与半透明预览。
- 顶部预留 **安全区**。

### 3.3 右栏（材料栏）

- 4 列格子背包，每格 4×4 小格，碎片居中显示。
- 仅显示 **未放置** 的碎片，带编号（#1、#2…）。
- 可点击 **收回** 折叠为竖条「材料」标签。
- 顶部避开 **胶囊按钮** 安全区。
- 拖拽时 **禁止滚动**，避免与拖拽冲突。

---

## 4. 交互需求

### 4.1 材料栏 → 操作区

| 行为 | 说明 |
|---|---|
| 拿起 | 长按/拖动碎片，材料栏对应格变为半透明占位 |
| 拖拽 | 手指跟随移动，进入操作区后 **吸附到网格** |
| 放置 | 在操作区内松手 → 按网格吸附放置 |
| 取消 | 在材料区或其他非操作区松手 → **归位** 回材料栏 |
| 选中 | 放置完成后 **不保持选中** |

### 4.2 操作区内移动

| 行为 | 说明 |
|---|---|
| 点击 | 点击已放置碎片 → **选中**，显示虚框与蒙层，可调层级 |
| 拖动 | 移动超过阈值（约 8px）→ 进入拖拽，碎片跟随并 **网格吸附** |
| 放置 | 在操作区内松手 → 吸附到新网格位置 |
| 取消 | 拖到操作区外松手 → **归位** 到原位置 |
| 选中 | 拖动结束后 **取消选中** |

### 4.3 层级调整

- 选中操作区碎片后，左栏显示工具栏：
  - **↑** 上移一层（置顶）
  - **↓** 下移一层
  - **收回** 移回材料栏
- 点击空白格或 **取消** 清除选中。

### 4.4 拖拽反馈

- 拿起时 **轻震动**。
- 拖拽中显示 **虚线边框** 包裹的浮动碎片。
- 进入操作区可放置时，面板边框 **高亮**（金色）。
- 选中/预览时，操作区 **局部蒙层**，选中块高亮显示。

### 4.5 其他操作

| 操作 | 说明 |
|---|---|
| 重置 | 二次确认后清空本关已放置碎片 |
| 提交 | 比对谜底，过关则弹窗 + 下一关 |
| 进度存档 | 本地存储当前关卡，下次进入继续 |

---

## 5. 数据与算法

### 5.1 网格规格

- 操作区 / 谜底：10×10
- 材料栏展示格：4×4（碎片居中）
- 碎片形状：多种多格形状（1~5 格），随机颜色

### 5.2 关卡生成流程

1. 按关卡种子随机生成正确碎片并叠加到空网格，形成谜底。
2. 生成干扰碎片。
3. 打乱全部碎片顺序并重新编号。
4. 存储：谜底、碎片列表、正确解（位置+层级）。

### 5.3 叠加规则

- 按 z 值从低到高绘制。
- 上层颜色完全覆盖下层。
- 判定以最终网格颜色与谜底逐格比较为准。

---

## 6. 技术约束

| 项 | 要求 |
|---|---|
| 框架 | 微信小程序原生 |
| 页面方向 | `landscape` 强制横屏 |
| 导航 | 自定义导航栏 |
| 渲染 | 操作区 Canvas 2D；谜底 View 格子渲染 |
| 拖拽 | WXS + 全屏拖拽层，减少卡顿 |
| 坐标 | 基于 Canvas 实际尺寸计算，抓取点偏移保持一致 |
| 存档 | `wx.setStorageSync('stackpuzzle_level')` |
| 最低版本 | 横屏需微信 8.0.31+ |

---

## 7. 非功能需求

- 操作区与谜底格子 **比例一致**，拖拽定位准确。
- 拖拽流畅，材料栏滚动不与拖拽冲突。
- 适配刘海、灵动岛、胶囊按钮安全区。
- 横屏三栏布局自适应不同屏幕宽度。

---

## 8. 功能清单（当前版本）

- [x] 20 关闯关与本地存档
- [x] 横屏三栏布局
- [x] 谜底固定预览（View 渲染）
- [x] 材料栏 4 列背包 + 可收起
- [x] 拖拽放置（材料栏 / 操作区互拖）
- [x] 网格吸附
- [x] 抓取点偏移（跟手）
- [x] 点击选中 + 层级调整
- [x] 选中/拖拽虚框与蒙层
- [x] 提交判定与过关弹窗
- [x] 重置本关
- [x] 安全区适配

---

## 9. 后续可扩展（未实现）

- 关卡星级 / 计时 / 提示系统
- 音效与动画 polish
- 关卡编辑器
- 分享与排行榜
- 云存档
